# 知识库研究助手 - 升级后的依赖清单
# 版本: v2.0 (推荐升级)
# 更新时间: 2026-01-29

# ========================================
# 核心框架
# ========================================
streamlit==1.40.2            # Web应用框架 (从 >=1.28.0 升级)
google-genai>=1.55.0         # Google Gemini API

# ========================================
# RAG & LLM 框架
# ========================================
langchain==0.3.16            # LLM应用框架 (从 >=0.1.0 升级)
langchain-google-genai==2.0.9  # Google集成 (从 >=0.0.6 升级)
langchain-community==0.3.16   # 社区集成 (从 >=0.0.13 升级)
langchain-core==0.3.42       # LangChain核心 (新增)

# ========================================
# 向量数据库
# ========================================
chromadb==0.5.30             # 向量数据库 (从 >=0.4.22 升级)

# ========================================
# 文档处理
# ========================================
pypdf==5.1.0                 # PDF处理 (替换 PyPDF2>=3.0.1)
python-docx>=1.1.0           # Word文档处理
unstructured==0.16.15        # 多格式文档处理 (从 >=0.11.0 升级)

# 可选：高级文档处理（如需要OCR和表格提取）
# docling==2.15.0             # IBM文档智能库
# pdfplumber==0.11.5          # 高精度PDF表格提取

# ========================================
# PPT生成
# ========================================
python-pptx==1.0.2           # PowerPoint生成 (从 >=0.6.23 升级)

# 可选：增强PPT功能
# Pillow>=11.1.0              # 图像处理
# matplotlib>=3.10.0          # 数据可视化

# ========================================
# 工具库
# ========================================
python-dotenv>=1.0.0         # 环境变量管理

# ========================================
# 测试和代码质量（新增）
# ========================================
pytest==8.3.4                # 测试框架
pytest-cov==6.0.0            # 测试覆盖率
black==24.10.0               # 代码格式化
ruff==0.8.5                  # 快速Linter
mypy==1.14.1                 # 类型检查

# ========================================
# 可选：性能增强
# ========================================
# sentence-transformers==3.3.1  # 用于本地Reranker
# fastembed==0.4.2              # 快速嵌入和重排序

# ========================================
# 可选：替代向量数据库（仅选择其一）
# ========================================
# pymilvus==2.4.10                  # Milvus Python客户端（高性能向量数据库）
# qdrant-client==1.13.0             # Qdrant向量数据库客户端

# ========================================
# 安装说明
# ========================================
# 标准升级:
#   pip install -r requirements-upgraded.txt
#
# 包含所有可选功能:
#   pip install -r requirements-upgraded.txt
#   pip install docling pdfplumber Pillow matplotlib
#   pip install sentence-transformers fastembed
#
# 性能要求高（替换ChromaDB为Milvus）:
#   # 先安装Milvus: docker run -d --name milvus -p 19530:19530 milvusdb/milvus:latest
#   pip install -r requirements-upgraded.txt
#   pip install pymilvus
